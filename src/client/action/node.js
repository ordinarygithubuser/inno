import { Action } from 'ive-f';

export const CreateNode = new Action();

export const UpdateNode = new Action();

export const DeleteNode = new Action();

export const SetNode = new Action();

export const SetPosition = new Action();